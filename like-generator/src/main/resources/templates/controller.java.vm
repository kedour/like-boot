package ${package.Controller};


import org.springframework.web.bind.annotation.*;
import org.springframework.beans.factory.annotation.Autowired;

import  ${package.Service}.${table.serviceName};


import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import io.swagger.annotations.ApiParam;
import lombok.extern.slf4j.Slf4j;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import ${cfg.packageName}.vo.${entity}VO;
import com.like.common.vo.JsonReturn;
#if(${restControllerStyle})
import org.springframework.web.bind.annotation.RestController;
#else
import org.springframework.stereotype.Controller;
#end
#if(${superControllerClassPackage})
import ${superControllerClassPackage};
#end

/**
 * <p>
 * $!{table.comment} 前端控制器
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
#if(${restControllerStyle})
@RestController
#else
@Controller
#end
@Api(value = "/api/${table.entityPath}", tags = "${table.controllerName}", description = "${table.comment}接口文档")
@RequestMapping(value = "#if(${package.ModuleName})/${package.ModuleName}#end/#if(${controllerMappingHyphenStyle})${controllerMappingHyphen}#else${table.entityPath}#end")
#if(${superControllerClass})
@Slf4j
public class ${table.controllerName} extends ${superControllerClass}<${entity}VO,Long> {
#else
public class ${table.controllerName} {
#end


    @Autowired
    public void setBusinessService(${entity}Service service){
        this.businessService=service;
    }

/**
 * 获取对象注释信息
 * @param
 * @return
 */
    @RequestMapping(value = "/getModel",method={RequestMethod.GET})
    @ApiOperation(value = "获取${table.comment}VO对象注释信息", response = ${entity}VO .class, notes = "获取${table.comment}对象注释信息")
    public JsonReturn getModel(){
        JsonReturn jsonReturn=new JsonReturn();
        return jsonReturn;
    }
}
